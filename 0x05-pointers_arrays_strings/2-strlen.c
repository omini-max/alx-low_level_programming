#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - program to check length of string
 * @s: character to check
 * Return: strlen
 */
int _strlen(char *s)
{
	s = strlen(s);
	return (s);
}
